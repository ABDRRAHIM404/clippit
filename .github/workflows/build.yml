name: Build Android Release APK

on:
  push:
    branches:
      - main
  workflow_dispatch: # Allows manual trigger of builds

jobs:
  build:
    name: Build & Release Release APK
    runs-on: ubuntu-latest
    
    # 🌟 CORRECT WAY: This tells GitHub that ALL steps inside this build
    # should be executed inside your "clippit" subfolder!
    defaults:
      run:
        working-directory: ./clippit

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Java Development Kit (JDK)
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.19.6'
          channel: 'stable'
          cache: true

      - name: Install Project Dependencies
        run: flutter pub get

      - name: Run Build Runner (Hive Adapters Generation)
        run: flutter pub run build_runner build --delete-conflicting-outputs

      - name: Build Release APK
        run: flutter build apk --release --no-shrink

      # 🌟 CRITICAL FIX: Since we compiled inside the subfolder,
      # the upload path must point to clippit/build/...
      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: clippit-release-apk
          path: clippit/build/app/outputs/flutter-apk/app-release.apk