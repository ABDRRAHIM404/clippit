import 'package:flutter/material.dart';

class AppColors {
  // Near-Black Dark Mode Backgrounds
  static const Color background = Color(0xFF0C0F12);
  static const Color surface = Color(0xFF161A1F);
  static const Color surfaceLight = Color(0xFF22282F);

  // High-Energy Primary Accent (Electric Violet / Purple)
  static const Color primary = Color(0xFF8A2BE2);
  static const Color primaryLight = Color(0xFFA044FF);

  // Secondary Accents & Dynamic States
  static const Color accentNeon = Color(0xFF00FFFF); // Electric Cyan (use for highlighted keywords)
  static const Color accentWarm = Color(0xFFFFFF00); // Electric Yellow (use for viral indicators)
  
  // Feedback & Status Colors
  static const Color success = Color(0xFF00FF87);
  static const Color error = Color(0xFFFF3366);
  static const Color warning = Color(0xFFFF9900);

  // Neutral Grays for Text & Icons
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFF8E99A5);
  static const Color textMuted = Color(0xFF5D6975);
  static const Color border = Color(0xFF2B333C);
}
